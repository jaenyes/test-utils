const add = (a: number, b: number) => {
  return a + b;
};

const subtract = (a: number, b: number) => {
  return a - b;
};

const trim = (phrase: string) => {
  return phrase.trim();
};

export { add, subtract, trim };
